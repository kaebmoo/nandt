# TODO List - Hospital Booking System
**วันที่:** 16 พฤศจิกายน 2025

## 🚨 หมายเหตุสำคัญ
**กรุณาอ่านก่อนทำงาน:**
- โค้ดปัจจุบันทำงานได้ดีอยู่แล้ว อย่าให้การแก้ไขมาสร้างปัญหาใหม่
- ทำทีละขั้นตอน ทดสอบให้แน่ใจก่อนไปขั้นตอนถัดไป
- สอบถามก่อนถ้าไม่แน่ใจ
- Backup หรือ commit ก่อนทำการแก้ไขที่สำคัญ

---

## 📋 สรุปภาพรวมระบบปัจจุบัน

### โครงสร้างหลัก
- **Flask App**: Web UI สำหรับ Admin Dashboard
- **FastAPI App**: REST API Backend
- **Shared DB**: SQLAlchemy Models สำหรับ Multi-tenant
- **Multi-tenant Architecture**: แยกข้อมูลแต่ละคลินิก/ผู้ให้บริการด้วย schema

### Models หลักที่เกี่ยวข้อง
1. **AvailabilityTemplate** - เทมเพลตตารางเวลาทำการ
2. **Provider** - ผู้ให้บริการ (แพทย์/ทันตแพทย์)
3. **TemplateProvider** - Mapping ระหว่าง template และ providers
4. **ProviderSchedule** - ตารางการทำงานของ provider
5. **EventType** - ประเภทการนัดหมาย
6. **Appointment** - การนัดหมาย

### Routes ที่เกี่ยวข้อง
- **Flask**: `/settings/availability` - หน้าจัดการ availability templates
- **FastAPI**: `/api/v1/tenants/{subdomain}/availability/*` - API endpoints

---

## 🔴 สิ่งที่ยังไม่ได้ทำ (Missing Features)

### 1. ระบบจัดการผู้ให้บริการ (Provider Management) - CRITICAL
**สถานะ:** ❌ ยังไม่มีเลย

**ปัญหา:**
- ไม่มีหน้า UI สำหรับจัดการผู้ให้บริการ (CRUD)
- มี Model `Provider` และ API endpoints อยู่แล้ว แต่ไม่มี Flask routes และ templates
- ปัจจุบันไม่สามารถ:
  - เพิ่มผู้ให้บริการใหม่ผ่าน UI
  - แก้ไขข้อมูลผู้ให้บริการ (ชื่อ, ตำแหน่ง, แผนก, email, phone, bio)
  - ลบผู้ให้บริการ
  - ดูรายชื่อผู้ให้บริการทั้งหมด
  - เปิด/ปิดการใช้งานผู้ให้บริการ (is_active)

**สิ่งที่ต้องทำ:**
1. สร้าง Flask Blueprint สำหรับ Provider Management
   - Route: `/settings/providers`
   - หน้าแสดงรายการผู้ให้บริการ (List)
   - หน้าเพิ่มผู้ให้บริการใหม่ (Create)
   - หน้าแก้ไขผู้ให้บริการ (Edit)
   - ฟังก์ชันลบผู้ให้บริการ (Delete)
   - ฟังก์ชันเปิด/ปิดการใช้งาน (Toggle Active)

2. สร้าง Templates
   - `templates/settings/providers/index.html` - หน้ารายการ
   - `templates/settings/providers/form.html` - form สำหรับ create/edit
   - เพิ่มลิงก์ใน navigation menu

3. สร้าง Forms (WTForms)
   - `ProviderForm` สำหรับ create/edit providers
   - Validation fields: name (required), title, department, email, phone, bio, is_active

4. ตรวจสอบ API Endpoints
   - ตรวจสอบว่า FastAPI มี endpoints ครบหรือไม่:
     - `GET /api/v1/tenants/{subdomain}/providers` - List providers
     - `POST /api/v1/tenants/{subdomain}/providers` - Create provider
     - `GET /api/v1/tenants/{subdomain}/providers/{id}` - Get provider details
     - `PUT /api/v1/tenants/{subdomain}/providers/{id}` - Update provider
     - `DELETE /api/v1/tenants/{subdomain}/providers/{id}` - Delete provider
   - ถ้าไม่มีให้สร้างใน `fastapi_app/app/providers.py`

**ไฟล์ที่ต้องสร้าง/แก้ไข:**
- `flask_app/app/provider_routes.py` (ใหม่)
- `flask_app/app/templates/settings/providers/index.html` (ใหม่)
- `flask_app/app/templates/settings/providers/form.html` (ใหม่)
- `flask_app/app/forms.py` (เพิ่ม ProviderForm)
- `flask_app/app/__init__.py` (register blueprint)
- `fastapi_app/app/providers.py` (อาจต้องสร้างใหม่)
- `fastapi_app/app/main.py` (register router)

**ลำดับความสำคัญ:** 🔴 CRITICAL - ต้องทำก่อนอื่น

---

### 2. ระบบจัดการประเภทบริการ (Service Type Management)
**สถานะ:** ⚠️ มี Model แต่ไม่มี UI

**ปัญหา:**
- มี Model `ServiceType` แต่ไม่มีหน้า UI จัดการ
- ไม่ทราบว่า ServiceType แตกต่างจาก EventType อย่างไร
- ไม่มีการใช้งานใน booking flow

**สิ่งที่ต้องทำ:**
1. สำรวจว่า ServiceType ใช้งานอย่างไร และจำเป็นหรือไม่
2. ถ้าจำเป็น ให้สร้าง UI คล้ายกับ Provider Management
3. ถ้าไม่จำเป็น ให้พิจารณาลบออกจาก Model

**ลำดับความสำคัญ:** 🟡 MEDIUM

---

### 3. ระบบรายงานและสถิติ (Reporting & Analytics)
**สถานะ:** ❌ ยังไม่มีเลย

**คำถาม:**
- ต้องการรายงานแบบไหนบ้าง?
  - สถิติการจองแยกตามผู้ให้บริการ
  - สถิติการจองแยกตามช่วงเวลา/วัน
  - สถิติการยกเลิก
  - Utilization rate ของผู้ให้บริการ
  - รายได้ (ถ้ามีระบบเก็บเงิน)

**ลำดับความสำคัญ:** 🟢 LOW - ทำทีหลัง

---

### 4. ระบบแจ้งเตือนและการส่งข้อความ
**สถานะ:** ⚠️ มี Service แต่ไม่ครบ

**ที่มีอยู่แล้ว:**
- `email_service.py` - ส่ง email
- `sms_service.py` - ส่ง SMS
- `otp_service.py` - OTP verification

**ที่ยังขาด:**
- Reminder notifications (แจ้งเตือนก่อนนัด)
- Automated emails/SMS
- Admin notifications
- Integration testing

**ลำดับความสำคัญ:** 🟡 MEDIUM

---

### 5. ระบบการชำระเงิน (Payment System)
**สถานะ:** ❌ ยังไม่มีเลย

**คำถาม:**
- ต้องการระบบเก็บเงินหรือไม่?
- Payment gateway ใด? (Stripe, PayPal, PromptPay, etc.)
- เก็บเงินมัดจำหรือเต็มจำนวน?

**ลำดับความสำคัญ:** 🟢 LOW - รอ requirement

---

### 6. Mobile App / Mobile Responsive
**สถานะ:** ⚠️ ต้องตรวจสอบ

**สิ่งที่ต้องทำ:**
- ทดสอบ responsive design บนมือถือ
- ตรวจสอบ public booking page บนมือถือ
- ปรับปรุง UI/UX ถ้าจำเป็น

**ลำดับความสำคัญ:** 🟡 MEDIUM

---

### 7. ระบบสิทธิ์และการจัดการผู้ใช้ (Role-Based Access Control)
**สถานะ:** ⚠️ มีบางส่วน

**ที่มีอยู่แล้ว:**
- Multi-tenant separation
- User login/authentication

**ที่ยังขาด:**
- Role-based permissions (Admin, Staff, Provider, etc.)
- Permission management UI
- Audit log

**ลำดับความสำคัญ:** 🟡 MEDIUM

---

### 8. ระบบ Waiting List
**สถานะ:** ❌ ยังไม่มีเลย

**คำถาม:**
- ต้องการ waiting list สำหรับช่วงเวลาที่เต็มหรือไม่?
- ระบบแจ้งเตือนเมื่อมีที่ว่าง?

**ลำดับความสำคัญ:** 🟢 LOW

---

### 9. การ Export ข้อมูล
**สถานะ:** ❌ ยังไม่มีเลย

**สิ่งที่ควรมี:**
- Export รายการนัดหมาย (Excel, CSV, PDF)
- Export รายงาน
- Backup/Restore functionality

**ลำดับความสำคัญ:** 🟡 MEDIUM

---

### 10. API Documentation
**สถานะ:** ⚠️ มี Auto-generated แต่อาจไม่สมบูรณ์

**สิ่งที่ต้องทำ:**
- ตรวจสอบ FastAPI Swagger UI (`/docs`)
- เพิ่ม descriptions, examples
- สร้าง API usage guide

**ลำดับความสำคัญ:** 🟡 MEDIUM

---

## 🟠 สิ่งที่ต้องปรับปรุง (Improvements Needed)

### 1. การเลือกผู้ให้บริการในเทมเพลต - IMPORTANT
**ไฟล์:** `flask_app/app/templates/settings/availability/index.html` (บรรทัด 673-738)

**ปัญหาปัจจุบัน:**
```html
<!-- Modal เพิ่มผู้ให้บริการ - ต้องเลือกทีละคน -->
<select name="provider_id" required>
    <option value="">-- เลือกผู้ให้บริการ --</option>
    {% for provider in provider_options %}
        <option value="{{ provider.id }}">{{ provider.name }}</option>
    {% endfor %}
</select>
```
- ต้องเลือกผู้ให้บริการทีละคน
- ถ้ามีผู้ให้บริการหลายคน ต้องเปิด modal หลายครั้ง
- ใช้งานยาก ไม่สะดวก

**วิธีแก้ไขที่เสนอ:**

**Option 1: Multiple Select + Batch Add (แนะนำ)**
```html
<select name="provider_ids" multiple size="10" required>
    <option value="">-- เลือกผู้ให้บริการ (กด Ctrl/Cmd เพื่อเลือกหลายคน) --</option>
    {% for provider in provider_options %}
        <option value="{{ provider.id }}" {% if provider.id in assigned_ids %}disabled{% endif %}>
            {{ provider.name }}{% if provider.title %} • {{ provider.title }}{% endif %}
        </option>
    {% endfor %}
</select>

<!-- Default settings สำหรับทุกคนที่เลือก -->
<label>
    <input type="checkbox" name="default_is_primary" value="on">
    กำหนดทุกคนเป็น Primary
</label>
<label>
    <input type="checkbox" name="default_can_auto_assign" value="on" checked>
    อนุญาตทุกคนให้ระบบเลือกอัตโนมัติ
</label>
<input type="number" name="default_priority" value="0">
```

**Option 2: Checkbox List**
```html
<div class="grid grid-cols-2 gap-2 max-h-64 overflow-y-auto">
    {% for provider in provider_options %}
        <label class="flex items-center space-x-2 p-2 border rounded">
            <input type="checkbox" name="provider_ids" value="{{ provider.id }}"
                   {% if provider.id in assigned_ids %}disabled{% endif %}>
            <span>{{ provider.name }}</span>
        </label>
    {% endfor %}
</div>
```

**Option 3: Select All + Individual Settings**
```html
<!-- ปุ่ม Select All -->
<button type="button" onclick="selectAllAvailableProviders()">
    เลือกผู้ให้บริการทั้งหมด
</button>

<!-- แสดงรายการที่เลือกพร้อมตั้งค่าแต่ละคน -->
<div id="selected-providers">
    <!-- Dynamically populated -->
</div>
```

**การแก้ไขโค้ด:**

**ไฟล์ที่ต้องแก้:**
1. `flask_app/app/templates/settings/availability/index.html`
   - แก้ไข Provider Modal (บรรทัด 673-738)
   - เพิ่ม JavaScript สำหรับ multiple select

2. `flask_app/app/availability_routes.py`
   - แก้ไข `add_template_provider()` function (บรรทัด 416-454)
   - รองรับการเพิ่มหลายคนพร้อมกัน

**โค้ดตัวอย่าง (Backend):**
```python
@availability_bp.route('/availability/template/<int:template_id>/providers/add-batch', methods=['POST'])
@login_required
def add_template_providers_batch(template_id):
    """เพิ่มผู้ให้บริการหลายคนพร้อมกัน"""
    provider_ids = request.form.getlist('provider_ids')  # รับเป็น list

    if not provider_ids:
        flash('กรุณาเลือกผู้ให้บริการอย่างน้อย 1 คน', 'error')
        return redirect_to_template_settings(template_id)

    # Default settings
    default_is_primary = request.form.get('default_is_primary') == 'on'
    default_can_auto_assign = request.form.get('default_can_auto_assign') == 'on'
    default_priority = int(request.form.get('default_priority', 0))

    success_count = 0
    errors = []

    for provider_id_str in provider_ids:
        try:
            provider_id = int(provider_id_str)
            payload = {
                'provider_id': provider_id,
                'is_primary': default_is_primary,
                'can_auto_assign': default_can_auto_assign,
                'priority': default_priority
            }
            _, error = make_api_request('POST', f'/availability/templates/{template_id}/providers', payload)

            if error:
                errors.append(f'Provider ID {provider_id}: {error}')
            else:
                success_count += 1
        except (TypeError, ValueError):
            errors.append(f'Invalid provider ID: {provider_id_str}')

    if success_count > 0:
        flash(f'เพิ่มผู้ให้บริการสำเร็จ {success_count} คน', 'success')

    if errors:
        for error in errors:
            flash(error, 'error')

    return redirect_to_template_settings(template_id)
```

**ลำดับความสำคัญ:** 🔴 CRITICAL

---

### 2. การเพิ่มตารางทำงานของผู้ให้บริการ - IMPORTANT
**ไฟล์:** `flask_app/app/templates/settings/availability/index.html` (บรรทัด 740-847)

**ปัญหาปัจจุบัน:**

1. **ต้องเลือกผู้ให้บริการใหม่อีกครั้ง:**
```html
<!-- Schedule Modal - ต้องเลือกผู้ให้บริการจาก dropdown ทั้งหมด -->
<select name="provider_id" id="schedule-provider-select" required>
    <option value="">-- เลือกผู้ให้บริการ --</option>
    {% for provider in provider_options %}  <!-- ทุกคนในระบบ -->
        <option value="{{ provider.id }}">{{ provider.name }}</option>
    {% endfor %}
</select>
```
**ควรจะเป็น:** เลือกได้เฉพาะผู้ให้บริการที่กำหนดในเทมเพลตนี้แล้ว

2. **ไม่มีเวลา default:**
```html
<!-- ต้องใส่เวลาเองทุกครั้ง -->
<input type="time" name="custom_start_time" id="schedule-start-time">
<input type="time" name="custom_end_time" id="schedule-end-time">
```
**ควรจะเป็น:** กรอกเวลาเบื้องต้นจากเทมเพลตให้อัตโนมัติ

**วิธีแก้ไข:**

**1. Filter ผู้ให้บริการให้เห็นเฉพาะคนที่อยู่ในเทมเพลต:**

```html
<select name="provider_id" id="schedule-provider-select" required>
    <option value="">-- เลือกผู้ให้บริการ --</option>
    {% for provider in provider_assignments %}  <!-- เปลี่ยนจาก provider_options -->
        <option value="{{ provider.provider_id }}"
                data-name="{{ provider.provider_name }}"
                {% if provider.provider_id in scheduled_provider_ids %}disabled{% endif %}>
            {{ provider.provider_name }}{% if provider.title %} • {{ provider.title }}{% endif %}
            {% if provider.provider_id in scheduled_provider_ids %}(มีตารางแล้ว){% endif %}
        </option>
    {% endfor %}
</select>
<p class="text-xs text-gray-500 mt-2">
    แสดงเฉพาะผู้ให้บริการที่กำหนดในเทมเพลตนี้แล้ว
</p>
```

**2. Auto-fill เวลาจากเทมเพลต:**

```javascript
// JavaScript สำหรับ auto-fill เวลา
function openScheduleModalForCreate() {
    const modal = document.getElementById('schedule-modal');
    const form = document.getElementById('provider-schedule-form');

    // Reset form
    form.reset();
    form.action = form.dataset.createAction;

    // Auto-fill template schedule times
    const templateSchedule = {{ schedule_map | tojson }};

    // หาช่วงเวลาที่ใช้บ่อยที่สุดในเทมเพลต (เช่น วันจันทร์)
    const mondaySchedule = templateSchedule['1'];
    if (mondaySchedule && mondaySchedule.length > 0) {
        const firstSlot = mondaySchedule[0];
        document.getElementById('schedule-start-time').value = firstSlot.start;
        document.getElementById('schedule-end-time').value = firstSlot.end;

        // เลือกวันจันทร์-ศุกร์ให้อัตโนมัติ
        for (let day = 1; day <= 5; day++) {
            const checkbox = document.querySelector(`input[name="days_of_week"][value="${day}"]`);
            if (checkbox) checkbox.checked = true;
        }
    }

    // Set default effective date to today
    const today = new Date().toISOString().split('T')[0];
    document.getElementById('schedule-effective-date').value = today;

    // Show modal
    openModal('schedule-modal');
}

// เพิ่ม tooltip แสดงว่ากำลังใช้เวลาจากเทมเพลต
function addTemplateTimeTooltip() {
    const startInput = document.getElementById('schedule-start-time');
    const endInput = document.getElementById('schedule-end-time');

    if (startInput.value && endInput.value) {
        const hint = document.createElement('p');
        hint.className = 'text-xs text-blue-600 mt-1';
        hint.textContent = '💡 เวลานี้ดึงมาจากเทมเพลต สามารถแก้ไขได้ตามต้องการ';
        startInput.parentElement.appendChild(hint);
    }
}
```

**3. เพิ่มปุ่ม "ใช้เวลาตามเทมเพลต":**

```html
<div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
    <div>
        <div class="flex items-center justify-between mb-2">
            <label class="text-sm font-medium text-gray-700">เวลาเริ่ม</label>
            <button type="button"
                    onclick="fillTemplateTime()"
                    class="text-xs text-blue-600 hover:text-blue-800">
                ใช้เวลาตามเทมเพลต
            </button>
        </div>
        <input type="time" name="custom_start_time" id="schedule-start-time">
    </div>
    <div>
        <label class="text-sm font-medium text-gray-700 mb-2">เวลาสิ้นสุด</label>
        <input type="time" name="custom_end_time" id="schedule-end-time">
    </div>
</div>
```

**ไฟล์ที่ต้องแก้:**
1. `flask_app/app/templates/settings/availability/index.html`
   - แก้ไข Schedule Modal (บรรทัด 740-847)
   - เพิ่ม JavaScript auto-fill functions
   - เปลี่ยน provider dropdown ให้ใช้ `provider_assignments` แทน `provider_options`

**ลำดับความสำคัญ:** 🔴 CRITICAL

---

### 3. ตรวจสอบการใช้งาน Template ที่ไม่สมบูรณ์ - IMPORTANT
**สถานะ:** ⚠️ ต้องตรวจสอบ

**คำถาม:**
1. ถ้าสร้าง template แต่ยังไม่ได้กำหนดผู้ให้บริการในเทมเพลต จะใช้งานได้หรือไม่?
2. ถ้าสร้าง template แต่ยังไม่ได้กำหนดตารางทำงานของผู้ให้บริการ จะใช้งานได้หรือไม่?
3. ผู้ใช้บริการสามารถจองใช้บริการได้หรือไม่ในกรณีข้างต้น?

**สิ่งที่ต้องทำ:**

1. **ตรวจสอบ Business Logic:**
   - ดู code ใน `fastapi_app/app/booking.py`
   - ดู code ใน `flask_app/app/public_booking.py`
   - ตรวจสอบ availability calculation logic

2. **Test Scenarios:**
   ```
   Test Case 1: Template without Providers
   - สร้าง template ใหม่
   - ไม่เพิ่มผู้ให้บริการ
   - ลองจองผ่าน public booking page
   - คาดว่า: ??

   Test Case 2: Template with Providers but No Schedules
   - สร้าง template ใหม่
   - เพิ่มผู้ให้บริการ
   - ไม่เพิ่มตารางทำงาน
   - ลองจองผ่าน public booking page
   - คาดว่า: ??

   Test Case 3: Template with template_type = 'pool'
   - สร้าง template type = 'pool' (ไม่ต้องระบุผู้ให้บริการ)
   - ลองจอง
   - คาดว่า: ควรจองได้โดยไม่ต้องเลือกผู้ให้บริการ
   ```

3. **เพิ่ม Validation และ Warnings:**

   **3.1 Validation ขณะสร้าง/แก้ไข Template:**
   ```python
   # ใน availability_routes.py หรือ availability.py (FastAPI)

   def validate_template_completeness(template):
       warnings = []
       errors = []

       # ถ้า template type = 'dedicated' แต่ไม่มีผู้ให้บริการ
       if template.template_type == 'dedicated' and not template.providers:
           warnings.append(
               'เทมเพลตนี้ต้องระบุผู้ให้บริการ (dedicated) แต่ยังไม่ได้กำหนด '
               'ผู้ใช้จะไม่สามารถจองได้จนกว่าจะเพิ่มผู้ให้บริการ'
           )

       # ถ้ามีผู้ให้บริการแต่ไม่มีตารางทำงาน
       if template.providers and not template.provider_schedules:
           warnings.append(
               'มีผู้ให้บริการแต่ยังไม่ได้กำหนดตารางทำงาน '
               'ระบบจะใช้เวลาทำการจากเทมเพลตสำหรับทุกคน'
           )

       # ถ้า requires_provider_assignment = True แต่ไม่มีผู้ให้บริการ
       if template.requires_provider_assignment and not template.providers:
           errors.append(
               'เทมเพลตนี้กำหนดให้ต้องเลือกผู้ให้บริการ แต่ยังไม่มีผู้ให้บริการในระบบ '
               'กรุณาเพิ่มผู้ให้บริการหรือเปลี่ยนเป็นไม่ต้องระบุผู้ให้บริการ'
           )

       return {'errors': errors, 'warnings': warnings}
   ```

   **3.2 แสดง Warnings ใน UI:**
   ```html
   <!-- ใน index.html -->
   {% if selected_template %}
       {% set validation = validate_template(selected_template) %}

       {% if validation.errors %}
           <div class="bg-red-50 border-l-4 border-red-500 p-4 mb-4">
               <h4 class="text-red-800 font-semibold">⚠️ เทมเพลตนี้ไม่สามารถใช้งานได้</h4>
               <ul class="list-disc list-inside text-red-700 text-sm mt-2">
                   {% for error in validation.errors %}
                       <li>{{ error }}</li>
                   {% endfor %}
               </ul>
           </div>
       {% endif %}

       {% if validation.warnings %}
           <div class="bg-yellow-50 border-l-4 border-yellow-500 p-4 mb-4">
               <h4 class="text-yellow-800 font-semibold">⚠️ คำเตือน</h4>
               <ul class="list-disc list-inside text-yellow-700 text-sm mt-2">
                   {% for warning in validation.warnings %}
                       <li>{{ warning }}</li>
                   {% endfor %}
               </ul>
           </div>
       {% endif %}
   {% endif %}
   ```

   **3.3 Badge Status:**
   ```html
   <!-- แสดง status badge บน template card -->
   <div class="template-item">
       <h3>{{ template.name }}</h3>

       {% if template.is_complete %}
           <span class="badge badge-success">✓ พร้อมใช้งาน</span>
       {% elif template.has_critical_issues %}
           <span class="badge badge-danger">✗ ไม่พร้อมใช้งาน</span>
       {% else %}
           <span class="badge badge-warning">⚠ ต้องตรวจสอบ</span>
       {% endif %}
   </div>
   ```

4. **Block Public Booking ถ้า Template ไม่พร้อม:**
   ```python
   # ใน public_booking.py

   def check_event_type_availability(event_type_id):
       event_type = get_event_type(event_type_id)
       template = event_type.template

       # Validate
       if template.requires_provider_assignment and not template.providers:
           return {
               'available': False,
               'reason': 'บริการนี้ยังไม่พร้อมให้บริการในขณะนี้ กรุณาติดต่อสอบถามเจ้าหน้าที่'
           }

       return {'available': True}
   ```

**ไฟล์ที่ต้องแก้/ตรวจสอบ:**
1. `fastapi_app/app/booking.py` - ตรวจสอบ availability logic
2. `flask_app/app/public_booking.py` - ตรวจสอบ booking flow
3. `flask_app/app/availability_routes.py` - เพิ่ม validation
4. `flask_app/app/templates/settings/availability/index.html` - แสดง warnings
5. `shared_db/models.py` - อาจต้องเพิ่ม helper methods

**ลำดับความสำคัญ:** 🔴 CRITICAL

---

### 4. Improve Error Handling and User Feedback
**สถานะ:** ⚠️ ต้องปรับปรุง

**ปัญหา:**
- Error messages บางที่ยังไม่เป็นมิตรกับผู้ใช้
- ไม่มี loading indicators ขณะทำงาน
- ไม่มี confirmation dialogs บางจุด

**สิ่งที่ต้องทำ:**
1. เพิ่ม Loading spinners/indicators
2. ปรับปรุง error messages ให้เข้าใจง่าย
3. เพิ่ม confirmation dialogs ก่อนลบ
4. Toast notifications แทน flash messages

**ลำดับความสำคัญ:** 🟡 MEDIUM

---

### 5. Performance Optimization
**สถานะ:** ⚠️ ควรตรวจสอบ

**สิ่งที่ต้องตรวจสอบ:**
1. Database queries - มี N+1 queries หรือไม่?
2. Caching - ควร cache อะไรบ้าง?
3. Indexing - Database indexes ครบหรือไม่?
4. API response time - ช้าเกินไปหรือไม่?

**เครื่องมือที่ใช้:**
- Flask-DebugToolbar
- Database query logging
- APM tools (New Relic, DataDog, etc.)

**ลำดับความสำคัญ:** 🟡 MEDIUM

---

### 6. Testing Coverage
**สถานะ:** ❌ น่าจะยังไม่มี

**สิ่งที่ต้องทำ:**
1. Unit tests สำหรับ business logic
2. Integration tests สำหรับ API endpoints
3. End-to-end tests สำหรับ booking flow
4. Load testing

**ลำดับความสำคัญ:** 🟡 MEDIUM

---

### 7. Documentation
**สถานะ:** ⚠️ ต้องปรับปรุง

**สิ่งที่ควรมี:**
1. User Manual - คู่มือการใช้งานสำหรับ Admin
2. Setup Guide - วิธีติดตั้งและ configuration
3. API Documentation - FastAPI Swagger docs
4. Database Schema Documentation
5. Deployment Guide

**ลำดับความสำคัญ:** 🟡 MEDIUM

---

### 8. Security Review
**สถานะ:** ⚠️ ต้องตรวจสอบ

**สิ่งที่ต้องตรวจสอบ:**
1. SQL Injection - ใช้ SQLAlchemy ถูกต้องหรือไม่?
2. XSS - Template rendering ปลอดภัยหรือไม่?
3. CSRF - มี CSRF token ครบหรือไม่?
4. Authentication - Session management ถูกต้องหรือไม่?
5. Authorization - Permission checks ครบหรือไม่?
6. Data validation - Input validation ครบหรือไม่?
7. Secrets management - API keys, passwords เก็บปลอดภัยหรือไม่?

**ลำดับความสำคัญ:** 🔴 CRITICAL

---

## 📝 ลำดับการทำงานที่แนะนำ

### Phase 1: Critical Fixes (ทำก่อน - สัปดาห์ที่ 1-2)
1. ✅ สร้าง TODO_20251116.md นี้
2. ⬜ ระบบจัดการผู้ให้บริการ (Provider Management) - CRITICAL
3. ⬜ ปรับปรุงการเลือกผู้ให้บริการในเทมเพลต (Multiple Select)
4. ⬜ ปรับปรุงการเพิ่มตารางทำงาน (Auto-fill from template)
5. ⬜ ตรวจสอบการใช้งาน Template ที่ไม่สมบูรณ์ + เพิ่ม Validation

### Phase 2: Important Improvements (สัปดาห์ที่ 3-4)
6. ⬜ Security Review + แก้ไขปัญหา
7. ⬜ Improve Error Handling and User Feedback
8. ⬜ Performance Optimization
9. ⬜ Mobile Responsive Testing

### Phase 3: Medium Priority (สัปดาห์ที่ 5-6)
10. ⬜ ระบบแจ้งเตือนและการส่งข้อความ (ทำให้สมบูรณ์)
11. ⬜ Role-Based Access Control
12. ⬜ Testing Coverage (Unit + Integration tests)
13. ⬜ Documentation

### Phase 4: Low Priority / Future Enhancements
14. ⬜ Service Type Management (ถ้าจำเป็น)
15. ⬜ Reporting & Analytics
16. ⬜ Payment System (ถ้าต้องการ)
17. ⬜ Waiting List
18. ⬜ Export ข้อมูล

---

## 🔍 สิ่งที่ต้องสอบถามผู้ใช้

### คำถามเกี่ยวกับ Business Requirements:
1. **Provider Management:**
   - ใคร้เป็นคนจัดการผู้ให้บริการ? (Admin only หรือ Staff ก็ทำได้?)
   - ต้องการ approval workflow หรือไม่?

2. **Template Usage:**
   - Template ที่ยังไม่สมบูรณ์ควรซ่อนจาก public booking หรือแสดงพร้อม warning?
   - ต้องการ draft mode หรือไม่?

3. **Scheduling:**
   - ถ้าผู้ให้บริการมีตารางทำงาน ควรใช้เวลาจากตารางหรือเวลาจากเทมเพลต?
   - Priority: Provider Schedule > Template Schedule?

4. **Features:**
   - Features ไหนที่ต้องการด่วนที่สุด?
   - Budget และ timeline เป็นอย่างไร?

5. **Payment:**
   - ต้องการระบบเก็บเงินหรือไม่?
   - ถ้าต้องการ ใช้ payment gateway ใด?

### คำถามเกี่ยวกับ Technical:
1. **Deployment:**
   - Deploy บน server ไหน?
   - มี CI/CD pipeline หรือไม่?

2. **Monitoring:**
   - มี monitoring/logging tools หรือไม่?
   - ต้องการ alerting หรือไม่?

3. **Backup:**
   - มี backup strategy หรือไม่?
   - Backup frequency?

---

## 📚 References

### ไฟล์สำคัญ
- **Models**: `/home/user/nandt/hospital-booking/shared_db/models.py`
- **Flask Routes**: `/home/user/nandt/hospital-booking/flask_app/app/availability_routes.py`
- **FastAPI Routes**: `/home/user/nandt/hospital-booking/fastapi_app/app/availability.py`
- **Templates**: `/home/user/nandt/hospital-booking/flask_app/app/templates/settings/availability/`
- **Forms**: `/home/user/nandt/hospital-booking/flask_app/app/forms.py`

### Database Tables (Tenant Schema)
- `availability_templates`
- `availabilities`
- `providers`
- `template_providers`
- `provider_schedules`
- `provider_leaves`
- `resource_capacities`
- `date_overrides`
- `event_types`
- `appointments`
- `patients`

### API Endpoints
- Base URL: `http://127.0.0.1:8000/api/v1/tenants/{subdomain}`
- Swagger Docs: `http://127.0.0.1:8000/docs`

---

## ⚠️ สิ่งที่ต้องระวัง

1. **Multi-tenant Data Isolation:**
   - ตรวจสอบให้แน่ใจว่าทุก query ใช้ tenant schema ถูกต้อง
   - ห้าม leak data ข้าม tenant

2. **Database Migrations:**
   - ทำ backup ก่อน migrate
   - ทดสอบใน development environment ก่อน

3. **Breaking Changes:**
   - ถ้าแก้ไข API ต้องตรวจสอบ backward compatibility
   - ถ้าแก้ไข Database schema ต้องมี migration script

4. **Performance:**
   - ระวัง N+1 queries
   - ใช้ eager loading เมื่อจำเป็น
   - เพิ่ม indexes ตาม query patterns

5. **Security:**
   - Validate input ทุกจุด
   - Sanitize output
   - ใช้ parameterized queries
   - ห้าม commit secrets ลง git

---

## ✅ Checklist ก่อนเริ่มทำงาน

- [ ] อ่าน TODO นี้ทั้งหมด
- [ ] เข้าใจ Architecture และ Codebase
- [ ] Setup Development Environment
- [ ] Backup Database
- [ ] Create Git Branch
- [ ] Clarify Requirements กับผู้ใช้
- [ ] เขียน Test Plan

---

## 📞 ติดต่อ / สอบถาม

ถ้ามีข้อสงสัยหรือต้องการความชัดเจนเพิ่มเติม กรุณาสอบถามก่อนเริ่มทำงาน

**Important:** อย่าลืม! โค้ดปัจจุบันทำงานได้ดีอยู่แล้ว เราต้องการปรับปรุง ไม่ใช่ทำลาย 🛡️

---

**Created:** 2025-11-16
**Last Updated:** 2025-11-16
**Version:** 1.0
